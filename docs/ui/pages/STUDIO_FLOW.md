# Time Wallpaper — STUDIO_FLOW.md

**Status:** Frozen

创作入口：

- 从图片创建
- AI 动态化

两条路径统一进入同一个 `ThemeDraft` / Theme Studio。

## Manual

允许 1–8 张进入 Draft。

缺失阶段显示空槽。

正式保存 / 导出 `.twtheme` 必须 8/8。

自动文件名排序仅识别：

- 01–08
- dawn / sunrise / morning / noon / afternoon / sunset / dusk / night

不做 AI 图像阶段猜测。

## Theme-level

V0.1 整个 Theme 共用：

- one Focal Point
- one Safe Area

## Save vs Export

- 保存 = 本地已安装 Theme
- 导出主题 = `.twtheme`

保存不等于应用。

## Ownership

内置 / 下载主题不能直接覆盖编辑。

必须“创建副本”，产生新 themeId。

## Recovery

需要 Draft Recovery。

只用于异常退出防丢，不做版本历史。

## AI

参考原图不自动进入最终 Theme。

AI metadata 不进入最终 Theme。

## Preview

Studio Preview 只在 App 内。

不临时修改真实 Windows 桌面。
